<div class="row-fluid">
  <div class="span8">
    <h2>Sign up</h2>
    <p class="lead">You're just a step away from <strong>taking action</strong> in your own town. Good for you! Once you're in, you have the power to clean, report and compete! Already have an account? <%= link_to "Sign in", new_user_session_path %>.</p>

    <%= simple_form_for(resource, :as => resource_name, :url => registration_path(resource_name)) do |f| %>
      <%= f.error_notification %>

      <div class="form-inputs">
        <%= f.input :email, :required => true, :autofocus => true %>
        <%= f.input :username, required: true %>
      <% if f.object.password_required? %>
        <%= f.input :password, :required => true %>
        <%= f.input :password_confirmation, :required => true %>
      <% end%>
      </div>

      <div class="form-actions">
        <%= f.button :submit, "Sign up" %>
      </div>
    <% end %>

    <%= render "devise/shared/links" %>
  </div>
  <div class="span4 hidden-phone">
    <div class="module emphasized-1">
      <h3>Before &amp; After</h3>
      <p><strong>SEE</strong> change happen. Reporters can attach photos to problems. Cleaners can attach after pictures. Instant proof of change. How rewarding!</p>
    </div>
    <div class="module emphasized-2">
      <h3>Get Notified!</h3>
      <p>We'll update you as soon as someone fixes a problem you reported. The feedback loop is wide open.</p>
    </div>
  </div>
</div>
