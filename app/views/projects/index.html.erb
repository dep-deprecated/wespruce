<h1>Current Problems</h1>
<p class="lead">
  Here's a map showing problems in your community.  Click on a problem to learn more about it.
<p>
<% if not user_signed_in? %>
  <p class="lead"><%= link_to "Create an account", new_user_registration_path %> and join the effort today!</p>
<% else %>
  <% if allow? 'project', 'new' %>
    <p class="lead">Or, you can <%= link_to 'Create a new project', new_project_path %> right now!</p>
  <% end %>
<% end if %>

<% if @projects.blank? %>
  <p>No open projects at this time.</p>
<% else %>
  <div id="project_map"></div><br/>
  <table border="0" cellspacing="0" cellpadding="0" width="100%" class="table table-striped table-bordered">
    <thead>
      <th>Project Name</th>
      <th>Project Description</th>
      <th>Status</th>
      <th>Rating<a href="#" class="help" title="<%= meaning_of_rating 0 %>">?</a></th>
    <tbody>
      <%= render @projects %>
    </tbody>
  </table>
<% end %>

<% if allow? 'project', 'new' %>
  <p><i class="icon-plus-sign"></i><%= link_to 'Create a new project', new_project_path %></p>
<% end %>

<%= paginate @projects %>
