<h1>Community Leaders</h1>
<p class="lead">Below are your community leaders. These people have gone above and beyond to dedicate time and energy to their community. The more problems you tackle, the more points you earn, and the higher your rank here!</p>
<table border="0" cellspacing="0" cellpadding="0" class="table table-striped table-bordered">
  <tr>
    <th>Rank</th>
    <th>Username</th>
    <th>Points</th>
  </tr>
  <% i = 1 %>
  <% for user in @users %>
    <tr>
      <td width="20"><%= i %></td>
      <td><%= link_to user.username, profile_path(user.username) %> <%= generate_badge user %></td>
      <td><%= user.points %></td>
    </tr>
    <% i = i + 1 %>
  <% end %>
</table>

<%= paginate @users %>
